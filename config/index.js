/* eslint-disable no-undef */
"use strict";
require("dotenv").config();
const {
  } = require("./initializer.json")

const {
  } = require('./contract.json')

const {
  } = process.env

module.exports = {
  };